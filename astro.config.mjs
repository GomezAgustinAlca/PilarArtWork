// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Usado para generar URLs absolutas en canonical/Open Graph.
  site: 'https://pilar-art-work.vercel.app',
});
